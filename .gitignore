# --- Obsidian 설정 전체 (기기마다 자동 생성/상이 → clone 충돌 방지) ---
# 설정/플러그인은 기기별로 따로 관리하고, git은 노트만 동기화
.obsidian/

# --- 트래시/리비전 (vault 내부 휴지통) ---
.trash/

# --- OS / 편집기 ---
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# --- 첨부 파일 중 대용량 ---
# (필요 시 활성화)
# 99-Attachments/*.pdf
# 99-Attachments/*.mp4

# --- 발표자료 대용량 바이너리 (모바일 sync 가볍게 유지) ---
*.pptx
30-Projects/Presentations/**/images/
